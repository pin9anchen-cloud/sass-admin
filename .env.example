# 复制为 .env.production（或在部署平台的环境变量里配置），本地开发不配置时默认走 localhost:8080

# 后端 HTTP 接口地址
REACT_APP_API_BASE_URL=https://api.yourdomain.com

# WebSocket 网关地址；不配置时会自动由 REACT_APP_API_BASE_URL 派生（http->ws，https->wss）
# 只有接口和 WS 网关不在同一个域名/协议时才需要单独配置
# REACT_APP_WS_BASE_URL=wss://ws.yourdomain.com
